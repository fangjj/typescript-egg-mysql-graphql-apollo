# import User from "user.graphql"
# 变更
type Mutation {
  # 创建用户
  createUser(
    name: String!
    gender: String!
    age: Int!
  ): User
  # 更新用户
  updateUser(
    id: Int!
    name: String
    gender: String
    age: Int
  ): User
  # 删除用户
  deleteUser(id: Int!): User
}